/**In this program we will
know about comment**/

#include<iostream> //The header file of C++
using namespace std; // we use namespace for the output function cout
int main(){ //program start to execute from this main function
    cout << "Hello World";  // Desired output of the program
    /** We have to write return 0 when we use int main()
    but if it was void as well as we use void main()
    then we don't need to use return 0**/
    return 0;
}
